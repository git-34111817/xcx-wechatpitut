//app.js
App({
  onLaunch: function () {
  },
  globalData:{
    aName_Domain : "//"
    // 这里如果想给域名赋值的话用
    //   var app = getApp();
    //   app.globalData.userInfo = res.data.data.uid
    // 如果想取出值用
    //   userId: getApp().globalData.userInfo
  }
})
